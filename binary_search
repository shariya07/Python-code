arr=[3,6,9,11,23,26,32,37,45,46,49,69]
x=int(input())
low = 0
high = len(arr) - 1
mid = 0
while low <= high:
  mid = (high + low) // 2
  # If x is greater, ignore left half
  if arr[mid] < x:
    low = mid + 1
  # If x is smaller, ignore right half
  elif arr[mid] > x:
    high = mid - 1
 # means x is present at mid
  else:
    print(mid)
 
# If we reach here, then the element was not present
print('No') 
 
